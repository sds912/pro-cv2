export interface Experience {
    jobTitle?: string;
    employer?: string;
    country?: string;
    city?: string;
    startDate?: string;
    endDate?: string;
    contract?: string;
    description?: string;
}